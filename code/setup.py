from setuptools import setup, find_packages

setup(
    name="optomammo_spie",
    version="0.1",
    description="package for the classification",
    packages=find_packages(),
    install_requires=[
        # Any dependencies go here.
    ],
)